<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap PUBLIC "-//iBATIS.com//DTD SQL Map 2.0//EN" "http://www.ibatis.com/dtd/sql-map-2.dtd">

<sqlMap namespace="SubPlanDesc">
	<typeAlias alias="SubPlanDesc" type="fitfame.po.SubPlanDesc" />

	<select id="getSubPlanDesc" resultClass="SubPlanDesc">
		select A.id,A.name, A.cid,
		A.intro, A.pic, A.url, A.category, B.quantity, A.units, B.duration,
		A.share, A.inuse, B.rank from
		sub_plan_desc as A , relation_splan_and_desc as
		B where B.spid = #value# and A.id = B.did order by B.rank asc
	</select>

	<select id="getCoachOwnPlanDesc" resultClass="SubPlanDesc">
		select * from sub_plan_desc where cid = #value# and inuse = 0
	</select>

	<select id="getSharePlanDesc" resultClass="SubPlanDesc">
		select * from sub_plan_desc where cid = #value# and inuse = 0
	</select>
	
	<insert id="insertSubPlanDesc" parameterClass="SubPlanDesc">
		insert into sub_plan_desc values (null, #cid#, #name#, #intro#, #pic#,
		#url#,
		#category#, #quantity#, #units#, #duration#, #share#, #inuse#, #rank#)
		<selectKey resultClass="long" type="post" keyProperty="id">
			select LAST_INSERT_ID() as value
		</selectKey>
	</insert>

	<update id="updateSubPlanDesc" parameterClass="SubPlanDesc">
		update sub_plan_desc
		<dynamic prepend="SET ">
			<isNotEmpty property="intro" prepend=","><![CDATA[ intro=#intro# ]]></isNotEmpty>
			<isNotEmpty property="pic" prepend=","><![CDATA[ pic=#pic# ]]></isNotEmpty>
			<isNotEmpty property="url" prepend=","><![CDATA[ url=#url# ]]></isNotEmpty>
			<isNotEqual property="category" compareValue="-1" prepend=","><![CDATA[ category=#category# ]]></isNotEqual>
			<isNotEqual property="quantity" compareValue="-1" prepend=","><![CDATA[ quantity=#quantity# ]]></isNotEqual>
			<isNotEmpty property="units" prepend=","><![CDATA[ units=#units# ]]></isNotEmpty>
			<isNotEqual property="duration" compareValue="-1" prepend=","><![CDATA[ duration=#duration# ]]></isNotEqual>
			<isNotEqual property="share" compareValue="-1" prepend=","><![CDATA[ share=#share# ]]></isNotEqual>
			<isNotEqual property="inuse" compareValue="-1" prepend=","><![CDATA[ inuse=#inuse# ]]></isNotEqual>
			<isNotEqual property="rank" compareValue="-1" prepend=","><![CDATA[ rank=#rank# ]]></isNotEqual>
		</dynamic>
		<dynamic prepend="WHERE">
			id = #id#
		</dynamic>
	</update>
</sqlMap>